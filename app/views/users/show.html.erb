<h2>User info</h2>

<h2>New book</h2>

<h2>Book detail</h2>


<table>
<% @books.each do |book| %>
<tr>
  <td><%= image_tag book.user.get_profile_image(50,50) %></td>
  <td><%= link_to book.title, book_path(book.id) %></td>
  <td><%= book.body %></td>
</tr>
 
<% end %>
</table>
